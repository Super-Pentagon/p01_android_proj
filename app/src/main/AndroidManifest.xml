<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.pentagon.p01_android_proj">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".P01Application"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".detail.ProductDetailActivity"></activity>
        <activity android:name=".login.forget.ResetPassActivity" />
        <activity android:name=".login.forget.ForgetPassActivity" />
        <activity android:name=".login.register.RegisterActivity" />
        <activity android:name=".login.login.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".flutter.activity.P01FragmentActivity" />
        <activity android:name=".search.ProductSearchActivity"></activity>
    </application>

</manifest>